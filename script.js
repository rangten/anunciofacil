
function buscarRanking() {
    const produto = document.getElementById('produto').value;
    const resultado = document.getElementById('resultado');
    resultado.innerHTML = 'Simulação: Buscando ranking de "' + produto + '"...';
    // Aqui será implementada a integração real com a API do Mercado Livre
}
